{
  "name": "denjixstream",
  "version": "1.0.0",
  "description": "Free, ad-proof movie & TV streaming — a single-file Cloudflare Worker with an Apple liquid-glass UI.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "serve": "node test-server.mjs",
    "deploy": "node deploy.mjs",
    "verify": "node --check worker.js && node build.js && node --check worker.js"
  },
  "keywords": [
    "cloudflare-workers",
    "streaming",
    "tmdb",
    "movie",
    "spa",
    "liquid-glass"
  ],
  "license": "MIT"
}
